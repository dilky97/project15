import { ServiceProviderDetails } from './service-provider-details.model';

describe('ServiceProviderDetails', () => {
  it('should create an instance', () => {
    expect(new ServiceProviderDetails()).toBeTruthy();
  });
});
